  <%= form_with(model:[bookdetail, book_comment], local:false ) do|f| %>
    <div class="form-group">
      <%= f.text_area :book_comment,style: "width:100%"%>
    </div>
    <div class="form-group">
      <%= f.submit "送信" %>
    </div>
  <% end %>